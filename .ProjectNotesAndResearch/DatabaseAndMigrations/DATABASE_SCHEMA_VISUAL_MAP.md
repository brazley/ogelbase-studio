# OgelBase Database Schema - Visual Entity Relationship Map

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        OGELBASE PLATFORM SCHEMA                              │
│                           (platform schema)                                   │
└─────────────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────────────────┐
│  MIGRATION 001 - CORE PLATFORM                                               │
└──────────────────────────────────────────────────────────────────────────────┘

    ┌─────────────────────┐
    │  organizations      │ ✅ EXISTS
    ├─────────────────────┤
    │ id (PK)             │
    │ name                │
    │ slug (UNIQUE)       │────────┐
    │ billing_email       │        │
    │ created_at          │        │
    │ updated_at          │        │
    └─────────────────────┘        │
              │                     │
              │ 1                   │
              │                     │
              │                     │
              │ N                   │
              ↓                     │
    ┌─────────────────────┐        │
    │  projects           │ ✅ EXISTS
    ├─────────────────────┤        │
    │ id (PK)             │        │
    │ organization_id (FK)│────────┘
    │ name                │
    │ ref (UNIQUE)        │
    │ database_host       │
    │ database_port       │
    │ database_name       │
    │ database_user       │
    │ database_password   │
    │ postgres_meta_url   │
    │ supabase_url        │
    │ status              │
    └─────────────────────┘
              │
              │ 1
              │
              │
              │ 1
              ↓
    ┌─────────────────────┐
    │  credentials        │ ✅ EXISTS
    ├─────────────────────┤
    │ id (PK)             │
    │ project_id (FK)     │
    │ anon_key            │
    │ service_role_key    │
    │ jwt_secret          │
    └─────────────────────┘

┌──────────────────────────────────────────────────────────────────────────────┐
│  MIGRATION 002 - BILLING & RESOURCES                                         │
└──────────────────────────────────────────────────────────────────────────────┘

    ┌─────────────────────┐
    │  subscriptions      │ ✅ EXISTS
    ├─────────────────────┤
    │ id (PK)             │
    │ organization_id (FK)│→ organizations
    │ plan_id             │→ ❌ billing_plans (MISSING!)
    │ plan_name           │
    │ status              │
    │ billing_cycle_anchor│
    │ current_period_*    │
    │ stripe_*            │
    └─────────────────────┘

    ┌─────────────────────┐
    │  invoices           │ ✅ EXISTS
    ├─────────────────────┤
    │ id (PK)             │
    │ organization_id (FK)│→ organizations
    │ stripe_invoice_id   │
    │ amount_due          │
    │ status              │
    │ period_start/end    │
    └─────────────────────┘

    ┌─────────────────────┐
    │  payment_methods    │ ✅ EXISTS
    ├─────────────────────┤
    │ id (PK)             │
    │ organization_id (FK)│→ organizations
    │ stripe_payment_*    │
    │ type                │
    │ card_*              │
    │ is_default          │
    └─────────────────────┘

    ┌─────────────────────┐
    │  usage_metrics      │ ✅ EXISTS
    ├─────────────────────┤
    │ id (PK)             │
    │ organization_id (FK)│→ organizations
    │ project_id (FK)     │→ projects
    │ metric_type         │
    │ metric_value        │
    │ period_start/end    │
    │ cost                │
    └─────────────────────┘

    ┌─────────────────────┐
    │  addons             │ ⚠️  CONFUSED DESIGN
    ├─────────────────────┤   (Should be addon_catalog)
    │ id (PK)             │
    │ project_id (FK)     │→ projects ⚠️ Should not be here!
    │ addon_type          │
    │ addon_variant       │
    │ quantity            │   ⚠️ This is USAGE data
    │ status              │   not catalog data!
    └─────────────────────┘

    ┌─────────────────────┐
    │  disk_config        │ ✅ EXISTS
    ├─────────────────────┤
    │ id (PK)             │
    │ project_id (FK)     │→ projects
    │ size_gb             │
    │ io_budget           │
    │ autoscale_*         │
    └─────────────────────┘

    ┌─────────────────────┐
    │  compute_config     │ ✅ EXISTS
    ├─────────────────────┤
    │ id (PK)             │
    │ project_id (FK)     │→ projects
    │ instance_size       │
    │ cpu_cores           │
    │ memory_gb           │
    └─────────────────────┘

┌──────────────────────────────────────────────────────────────────────────────┐
│  MIGRATION 003 - MULTI-DATABASE (Exists, but separate)                      │
└──────────────────────────────────────────────────────────────────────────────┘

    ┌─────────────────────┐
    │  databases          │ ⚠️  EXISTS but not in main migration path
    ├─────────────────────┤
    │ id (PK)             │
    │ project_id (FK)     │→ projects
    │ name                │
    │ type                │ (postgres/redis/mongodb)
    │ connection_string   │
    │ status              │
    │ health_check_*      │
    └─────────────────────┘
              │
              │ 1
              │
              │ N
              ↓
    ┌─────────────────────┐
    │ database_metrics    │ ⚠️  EXISTS but not in main path
    ├─────────────────────┤
    │ id (PK)             │
    │ database_id (FK)    │
    │ metric_type         │
    │ value               │
    │ timestamp           │
    └─────────────────────┘

┌──────────────────────────────────────────────────────────────────────────────┐
│  MIGRATION 003 - CRITICAL MISSING TABLES                                     │
│  (Should be created BEFORE migration 004)                                    │
└──────────────────────────────────────────────────────────────────────────────┘

    ┌─────────────────────┐
    │  users              │ ❌ MISSING
    ├─────────────────────┤    (Foundation for all user_id FK)
    │ id (PK)             │
    │ email (UNIQUE)      │
    │ username            │
    │ first_name          │
    │ last_name           │
    │ password_hash       │
    │ mfa_enabled         │
    └─────────────────────┘
              │
              │ Referenced by
              │ (via user_id FK)
              │
    ┌─────────┴───────────┬────────────────┬─────────────────┐
    │                     │                │                 │
    ↓                     ↓                ↓                 ↓
┌────────────────┐  ┌────────────────┐  ┌──────────────┐  ┌──────────────┐
│ organization_  │  │ project_       │  │ user_        │  │ organization_│
│ members        │  │ members        │  │ sessions     │  │ invitations  │
├────────────────┤  ├────────────────┤  ├──────────────┤  ├──────────────┤
│ id (PK)        │  │ id (PK)        │  │ id (PK)      │  │ id (PK)      │
│ organization_id│→org│ project_id  │→prj│ user_id (FK)│  │ organization_│→org
│ user_id (FK)   │  │ user_id (FK)   │  │ token        │  │ invited_by   │
│ role           │  │ role           │  │ expires_at   │  │ invited_email│
└────────────────┘  └────────────────┘  └──────────────┘  └──────────────┘
     ❌ MISSING          ❌ MISSING         ❌ MISSING         ❌ MISSING
  🔴 BLOCKS 004!

    ┌─────────────────────┐
    │  billing_plans      │ ❌ MISSING
    ├─────────────────────┤    (Referenced by subscriptions)
    │ id (PK)             │
    │ name                │
    │ price               │
    │ interval            │
    │ features (JSONB)    │
    │ max_projects        │
    └─────────────────────┘
              ↑
              │ Should be referenced by
              │ subscriptions.plan_id
              │ (Currently no FK!)

    ┌─────────────────────┐
    │  addon_catalog      │ ❌ MISSING
    ├─────────────────────┤    (Rename existing addons table)
    │ addon_id (PK)       │
    │ name                │
    │ description         │
    │ base_price          │
    │ interval            │
    └─────────────────────┘
              │
              │ 1
              │
              │ N
              ↓
    ┌─────────────────────┐
    │  project_addons     │ ❌ MISSING
    ├─────────────────────┤    🔴 API BROKEN!
    │ id (PK)             │    /api/platform/projects/[ref]/billing/addons
    │ project_id (FK)     │→ projects
    │ addon_id (FK)       │→ addon_catalog
    │ quantity            │
    │ status              │
    └─────────────────────┘

    ┌─────────────────────┐
    │  project_metrics    │ ❌ MISSING
    ├─────────────────────┤    🔴 API BROKEN!
    │ id (PK)             │    /api/platform/projects/[ref]/infra-monitoring
    │ project_id (FK)     │→ projects
    │ timestamp           │
    │ cpu_usage           │
    │ memory_usage        │
    │ disk_io_budget      │
    └─────────────────────┘

    ┌─────────────────────┐
    │  api_keys           │ ❌ MISSING
    ├─────────────────────┤    (Recommended)
    │ id (PK)             │
    │ organization_id (FK)│→ organizations
    │ user_id (FK)        │→ users
    │ key_hash            │
    │ scopes (JSONB)      │
    │ expires_at          │
    └─────────────────────┘

    ┌─────────────────────┐
    │  audit_logs         │ ❌ MISSING
    ├─────────────────────┤    (Recommended)
    │ id (PK)             │
    │ organization_id (FK)│→ organizations
    │ project_id (FK)     │→ projects
    │ user_id (FK)        │→ users
    │ event_type          │
    │ resource_type       │
    │ action              │
    │ metadata (JSONB)    │
    └─────────────────────┘

┌──────────────────────────────────────────────────────────────────────────────┐
│  MIGRATION 004 - DATA SEED (Currently FAILS!)                               │
└──────────────────────────────────────────────────────────────────────────────┘

Migration 004 tries to INSERT into:
  ✅ organizations (exists)
  ✅ projects (exists)
  ❌ organization_members (MISSING! - causes failure on line 40)

Error: relation "platform.organization_members" does not exist

┌──────────────────────────────────────────────────────────────────────────────┐
│  DEPENDENCY GRAPH                                                            │
└──────────────────────────────────────────────────────────────────────────────┘

Migration Order:
  001 → 002 → ❌ 003 (missing!) → 004 (fails!)
                  ↑
                  │
                  └─── CRITICAL GAP HERE

Correct Order Should Be:
  001 → 002 → 003 (new!) → 004 ✅ → 005 (current 003 multi-db)

┌──────────────────────────────────────────────────────────────────────────────┐
│  API ENDPOINT STATUS                                                         │
└──────────────────────────────────────────────────────────────────────────────┘

Working (with DB):
  ✅ GET /api/platform/organizations
  ✅ GET /api/platform/organizations/[slug]
  ✅ GET /api/platform/projects
  ✅ GET /api/platform/projects/[ref]
  ✅ POST /api/platform/projects/create

Broken (queries missing tables):
  ❌ GET /api/platform/organizations/[slug]/usage
     → Queries: platform.organization_members (line 69)

  ❌ GET /api/platform/projects/[ref]/billing/addons
     → Queries: platform.project_addons (lines 96-98)

  ❌ GET /api/platform/projects/[ref]/infra-monitoring
     → Queries: platform.project_metrics (lines 69-70)

Returns Mock Data:
  🟡 GET /api/platform/profile
     → Hardcoded: {id: 1, email: 'admin@ogelbase.com'}

  🟡 GET /api/platform/organizations/[slug]/billing/plans
     → Queries: platform.billing_plans (missing, returns defaults)

┌──────────────────────────────────────────────────────────────────────────────┐
│  SUMMARY                                                                     │
└──────────────────────────────────────────────────────────────────────────────┘

Current State:
  ✅ 13 tables from migrations 001 + 002
  ⚠️  3 tables from migration 003 (multi-db, not applied)
  ❌ 11 tables missing (should be in migration 003)
  ───────────────────────────────────────────────────
  Total: 27 tables needed for complete platform

Critical Impact:
  🔴 Migration 004 cannot run
  🔴 3 API endpoints broken
  🔴 No user/member management
  🔴 No access control
  🔴 Security gaps

Recommended Action:
  1. Create Migration 003 with 11 missing tables
  2. Apply Migration 003
  3. Re-run Migration 004 (should succeed)
  4. Optionally apply current 003 as Migration 005

Estimated Time: 2-3 hours for complete implementation
```
